# Environment based workflow

# name: Deploy to AWS Lambda (dev)
# on:
#   push: 
#     branches: [ develop ]

# jobs:
#   deploy:
#     name: Deploy to Lambda (dev)
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         python-version: [3.8]

#     environment:
#       name: dev

#     steps:
#     - name: Checkout Code
#       uses: actions/checkout@v2

#     - name: Set up Python ${{ matrix.python-version }}
#       uses: actions/setup-python@v2
#       with:
#         python-version: ${{ matrix.python-version }}

#     - name: Install Requirements
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt

#     - name: Test with pytest
#       run: pytest

#     - name: Create Zip file
#       uses: montudor/action-zip@v0.1.0
#       with:
#         args: zip -qq -r ./bundle.zip lambda_function.py LTVCalculator.py

#     - name: Deploy
#       uses: aws-actions/configure-aws-credentials@v1
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: eu-west-2

#     - name: Deploy to Lambda
#       run: aws lambda update-function-code --function-name ${{ secrets.FUNCTION_NAME }} --zip-file fileb://bundle.zip
